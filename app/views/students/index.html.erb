
<h3> Stusents index </h3>


<style>
  .table td{
    padding-right: 5px; /* Adjust the desired amount of space */
  }
  .table th {
    padding-right: 40px; /* Adjust the desired amount of space */
  }
  table, th, td {
  border: 2px solid black;
  border-collapse: collapse;
</style>

<!-- <h1>All comment of link and button</h1> -->
<div class="table table-dark table-striped">
  <!-- <button class="btn btn-primary" onclick="location.href='<%= new_student_path %>'" role="button"> Student Record</button> -->
  <!-- <a class="btn btn-primary" href="<= new_student_path %>" role="button">Create a  recode
  </a> -->
    <!-- <= link_to("Create a <source src="" type="">ode", '/students/new', :class => 'action edit' ) %> -->
    <!-- <= link_to("Create a Student recode", new_student_path, class: 'btn btn-primary') %> -->
    <!-- <button class="btn btn-primary" onclick="location.href='<%= new_section_path %>'" role="button">Section Record</button>
    <br>  
    <button class="btn btn-primary" onclick="location.href='<%= sections_path %>'" role="button">Section index Record</button>
 -->
 <br>
    
<table class="table">
	<thead>
		<tr>
  			<th>ID</th>
    		<th>Name</th>
   			<th>Age</th>
    		<th>Father_name</th>
   			<th>Phone_number</th>
    		<th>Email</th>
    		<th>Blood_group</th>
    		<th>Section id</th>
    		<th>Show</th>	
   			<th>Edit</th>
    		<th>Delete</th>
  		</tr>
  	</thead>
  	<tbody>	
  		<% @students.each do |student|%>
  			<tr>
  				<td><%= student.id %></td>  
    			<td><%= student.name %></td>
    			<td><% if student.age <= 60 %>
            <%= student.age %>
            <% else %>
            <script>
              alert("Error: Student range must be less than or equal to 60.");
            </script>
            Maximum limit exceeded
            <% end %>
          </td>
    			<td><%= student.father_name %></td>
    			<td>
            <% if student.phone_number.present? %>
              <% student_phone_number = "#{student.phone_number[0..3]}- #{student.phone_number[4..10]}" %>
              <%= student_phone_number %>
             <% else %>
              No phone number provided
            <% end %>
          </td>
   				<td><%= student.email %></td> 
    			<td><% if /(A\+|A\-|B\+|B\-|AB\+|AB\-|O\+|O\-)/.match?(student.blood_group) %>
            <%= student.blood_group %>
            <% else %>
            <script>
              alert("enter a correct blood group like A+")
            </script>
            invalid blood group
            <% end %>

              
            </td>
    			<td><%= student.section&.id %></td>

    			<td class="actions">
    			<!-- <= link_to("Show", '/students', :class => 'action show' ) %> -->
    			<%= link_to 'Show', student_path(student), :class => 'action show' %></td>
    			<td class="actions">	
    				<%= link_to("Edit", edit_student_path(student),:data => { confirm: "Are you sure you want to Edit this student?" }, :class => 'action edit' ) %>
    			</td>
    			<td class="actions">	
    				<%= link_to("Delete", student_path(student),:method => :delete,:data => { confirm: "Are you sure you want to delete this student?" },
    			 :class => 'action delete' ) %>
    			</td>
  			</tr>
  	<% end %>
	</tbody>
</table>
</div>
